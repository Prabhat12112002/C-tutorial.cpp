#include<bits/stdc++.h>
using namespace std;

int main(){
    string s;
    cout<<"Enter permission - \n";
    cin>>s;
    if(s=="yes" || s=="ok" || s=="comein"){
        cout<<"Mam permit to Enter the class.";
    }
    else if(s=="no"){
        cout<<"Mam does not allow to Enter the class.";
    }
    else{
        cout<<"Mam does not allow to Enter the class.";
    }

    return 0;
}